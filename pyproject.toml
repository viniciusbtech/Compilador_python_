[project]
name = "jss-compiler"
version = "0.1.0"
description = "Compilador acadêmico para Java Script Simplificado (JSS)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
